# PR #36 Local Validation Evidence — 5953286

**Validated code SHA:** `595328650830206f3d6d6f5e0a2ea5fed6665ece`
**Execution UTC:** `2026-09-03T19:07:40.532386+00:00`
**Host:** NOAR Windows VM
**Log path:** `%USERPROFILE%\Documents\Codework-validation-5953286.log`
**Log SHA-256:** `0E882329AEF91A0B9C3EFA247C3330C19E617445543FE7AA652A2DD197FB4511`

The log is external to the repository so validation itself cannot dirty the tested checkout. Its complete bytes are integrity-pinned above. The Python environment was the isolated `%USERPROFILE%\Documents\Codework-local-venv`, populated from the repository-pinned `reporting/requirements.txt`.

## Commands and results

Executed from the repository root unless noted otherwise:

1. `%USERPROFILE%\Documents\Codework-local-venv\Scripts\python.exe scripts/validate_repo.py`
   - Exit code: `0`.
   - Repository contract and all validator banner checks reported `PASS` in the hashed log.
2. `%USERPROFILE%\Documents\Codework-local-venv\Scripts\python.exe scripts/verify_supply_chain_lock.py`
   - Exit code: `0`.
   - Immutable Actions, container digests, and pinned runtime checks reported `PASS` in the hashed log.
3. `%USERPROFILE%\Documents\Codework-local-venv\Scripts\python.exe -m unittest tests.test_ci_optimization_contract tests.test_workflow_contracts tests.test_codacy_workflow_security tests.test_repo_contract -v`
   - Exit code: `0`.
   - `35` tests executed; `0` failures and `0` errors.
4. `node --test tests/test_codacy_pr_comment.js`
   - Exit code: `0`.
   - `18` tests passed; `0` failed.
5. `npm run build` from `mcp/`
   - Exit code: `0`.
   - TypeScript compiler `tsc -p tsconfig.json` completed successfully.
6. `bash -n scripts/*.sh tests/test_wgs_align_or_stage.sh`
   - Exit code: `0`.
7. `git diff --check 67b3133b6dd881a120ebee2edd9435458674fef8...HEAD`
   - Exit code: `0`.
8. `git status --porcelain`
   - Exit code: `0`.
   - Output empty; the validated code checkout was clean.

The captured log terminates with `WORKTREE_CLEAN=true` and `VERIFICATION_EXIT=0`.

## Scope limitation

Docker is not installed on the NOAR host, so Linux/container runtime checks are not claimed as locally executed. GitHub Actions remains authoritative for those jobs on the final pushed SHA. A prior native-Windows full-suite comparison identified POSIX-specific baseline failures; this evidence deliberately makes no claim that the complete Windows suite passes.
